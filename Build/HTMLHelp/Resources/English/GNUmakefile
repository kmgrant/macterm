# note: this must match the name of this directory, because
# the rules that depend on files in here assume that (see
# ../GNUmakefile)
LANGUAGE = English

all:
	$(MAKE) -C ../.. build-$(LANGUAGE)

clean:
	$(MAKE) -C ../.. clean-$(LANGUAGE)

